(Given "^a var exists$"
  (lambda ()

    ))

(Given "^truth is true$"
  (lambda ()

    ))

(When "^I do something$"
  (lambda ()

    ))

(Then "^I should scream in joy$"
  (lambda ()
    (assert (equal t t))
    ))
